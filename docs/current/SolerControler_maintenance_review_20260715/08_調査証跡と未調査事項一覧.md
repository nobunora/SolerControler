# 調査証跡と未調査事項一覧

> 対象リポジトリ: `C:\VSC\SolerControler`  
> レビュー基準HEAD: `88c11ea` (`Embed dashboard bootstrap payload in HTML`)  
> 作成日: 2026-07-15  
> 文書の性格: 実装着手前の保守性改善設計書。確認済み事実と未確認事項を分離して記載する。  
> 注意: 本文書作成時点ではソースコード変更を行っていない。

## 1. 実行済み確認

### 1.1 Git状態

レビュー基準HEADは `88c11ea`。直近修正はdashboard bootstrap payloadのHTML埋込みである。作業ツリーには文書系の未追跡ディレクトリがあったが、今回のレビューではソース変更を行っていない。

### 1.2 テスト

```text
214 passed in 9.01s
```

pytest plugin自動読込を無効化した実行で成功した。通常実行はAI Pwsh Bridge側の20秒transport timeoutにより失敗したが、テスト自体の失敗ではない。

JavaScript:

```text
node tests/test_dashboard_calculations.js
ExitCode 0
```

### 1.3 mypy

```text
Found 237 errors in 23 files (checked 34 source files)
```

代表的な分類:

- untyped function/parameter
- `Any` return
- `None` unionの未絞込み
- dict型の不一致
- objectからfloat変換
- Firestore sync/async型のunion
- third-party stub差

mypyは現時点で全体品質ゲートとして通らない。

### 1.4 Python version

- `Dockerfile`: `python:3.12-slim`
- `Dockerfile.dashboard`: `python:3.12-slim`
- `pyproject.toml`: `python_version = "3.14"`

### 1.5 pre-release

`scripts/pre_release_check.ps1` は以下を実行する。

- requirements-dev install
- Firestore→SQLite validation sync
- compileall
- pytest
- security_check

mypy、JavaScript test、Docker buildは含まない。

## 2. 規模調査

主要production file:

| ファイル | 概算行数 |
|---|---:|
| `energy_model_main.py` | 2303 |
| `app/kpnet_workflow.py` | 1772 |
| `app/dashboard_data.py` | 1685 |
| `static/dashboard.js` | 1449 |
| `cloud_job_runner.py` | 1386 |
| `app/operations_db.py` | 1256 |
| `app/pv_array_forecast.py` | 1012 |
| `app/sheets_export.py` | 923 |
| `app/postgres_ops.py` | 808 |
| `app/soc_cost_optimizer.py` | 740 |
| `app/firestore_ops.py` | 715 |

主要長大関数:

| 関数 | 行数概算 |
|---|---:|
| `energy_model_main.main` | 518 |
| `KpNetClient` class | 259 |
| `_load_sqlite_slice` | 235 |
| `_load_postgres_slice` | 235 |
| `_monitor_partial_forced_and_stop` | 223 |
| `calibrate_performance_ratio` | 197 |
| `_load_firestore_slice` | 192 |
| `_run_settings_phase` | 188 |
| Firestore `recalc_cost_daily` | 180 |
| PostgreSQL `recalc_cost_daily` | 170 |
| SQLite `recalc_cost_daily` | 169 |

## 3. DB重複証跡

`recalc_cost_daily`:

- SQLite: 169行、if 4、loop 6、try 1
- PostgreSQL: 170行、if 4、loop 6、try 1
- Firestore: 180行、if 9、loop 8、try 1

3実装とも `_parse_hhmm_to_minute`, `_tiered_day_increment_cost`, `_is_within_window` 等を呼ぶ。

`ingest_sunshine_from_night_plan`:

- SQLite: 159行
- PostgreSQL: 136行
- Firestore: 120行

`upsert_battery_daily_metrics`:

- SQLite: 66行
- PostgreSQL: 67行
- Firestore: 41行

PostgreSQL/Firestoreは `app.operations_db` のprivate helperをimportする。

## 4. 未使用・未接続候補

静的参照調査で定義以外のproduction参照が見つからなかった候補:

- `cloud_job_runner._read_plan_json`
- `cloud_job_runner._latest_soc_percent`
- `cloud_job_runner._sleep_with_progress`
- `app.db_sync._sqlite_upsert_row`

テストからのみ参照された候補:

- `_read_plan_snapshot`
- `_forecast_changed`
- `_refresh_plan_for_same_date_if_changed`

注意: 動的参照、外部script import、手動利用を完全には否定できない。削除前に全リポジトリ検索、CLI利用、文書、運用手順を確認する。

## 5. import構造

今回のASTベース調査では循環importは検出されなかった。主な方向:

- `dashboard_server -> app.dashboard_data`
- `cloud_job_runner -> constants, kpnet_workflow, night_plan_archive, soc_decision_feedback`
- `energy_model_main -> forecast/optimizer/model modules`
- `postgres_ops/firestore_ops -> operations_db private helpers`

最後の依存は循環ではないが、層の向きが不自然である。

## 6. 追加調査バックログ

### P0: 実機・安全

- forced charge中のjob crash復旧。
- KP-NET write/confirm失敗時の実状態。
- 二重job排他。
- standby fail-safeの機器仕様。

### P1: データ正確性

- backend同日データparity。
- tariff境界と締め日。
- Firestore daily reviewの定義。
- night plan schemaとconsumer一覧。
- current SOCの正本・fallback順位。

### P1: 運用

- backend正本方針。
- Cloud Job slotとtimeout。
- artifact retention/atomic write。
- backup/restore実演。

### P2: 保守性

- env一覧と利用実績。
- Python version方針。
- JS対応ブラウザ/CSP。
- plotting依存の本番必要性。
- dashboard cache性能。

## 7. 実装前チェックリスト

- [ ] 対象段階のcharacterization testを追加した。
- [ ] 本番環境変数と秘密情報をfixtureへ含めていない。
- [ ] 外部I/Oを伴う変更にfeature flagがある。
- [ ] 新旧結果比較方法がある。
- [ ] rollback手順がPRに記載されている。
- [ ] Python 3.12で実行・型検査している。
- [ ] backend固有差を「共通化漏れ」と誤認していない。
- [ ] 実機停止・standby経路をレビューした。
- [ ] pre-release local/integrationのどちらを通したか明記した。

## 8. 推奨PR単位

1. Characterization fixtures and parity test skeleton
2. Time/tariff primitives
3. Monitoring CSV and night plan models
4. Shared cost domain
5. Shared night plan/battery domain
6. Operations repository/service
7. Dashboard source models/service
8. Forced charge state models/transition tests
9. Forced charge executor integration
10. Energy plan payload builder
11. Energy plan stage extraction
12. KP-NET settings intent
13. Dashboard JS date/api/store modules

各PRは可能な限り500行前後の論理差分に抑え、機械的移動と仕様変更を分ける。
