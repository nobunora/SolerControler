## Summary

- What changed:
- Why:

## Verification

- [ ] Relevant focused tests passed
- [ ] Repository quality gate passed or any unrelated baseline failure is documented
- [ ] No secrets, local paths, generated artifacts, or debug bypasses were added

## Performance impact

Complete this section for dashboard, API, reporting, history, storage-read, or other user-facing read-path changes. Otherwise write `N/A`.

- Request/data path before:
- Request/data path after:
- Initial/default-view request count before / after:
- Compressed transfer size before / after:
- Backend reads or scans before / after:
- Cache / ETag behavior:
- History or bulk-data lazy-loading behavior:
- Materialized-history invalidation/rebuild behavior:
- Production-only measurement still required:

Do not claim unmeasured production latency, transfer size, or backend-read reductions as measured results.

## Production / external effects

- Deployment scope:
- IAM / secret / scheduler / device impact:
- Rollback or fallback:
